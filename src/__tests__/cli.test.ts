import { describe, it, expect } from 'vitest'; // or '@jest/globals'
import { functionName } from './cli';

describe('cli', () => {
  it('should ', () => {
    // Arrange

    // Act

    // Assert
    expect(true).toBe(true);
  });
});
